{
  "name": "zarara",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter acyclic-output-fuzz build:release",
    "build:release": "pnpm --filter acyclic-output-fuzz build:release",
    "build:dev": "pnpm --filter acyclic-output-fuzz build:dev"
  },
  "engines": {
    "node": ">= 18"
  },
  "packageManager": "pnpm@11.1.2"
}
