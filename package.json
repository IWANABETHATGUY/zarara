{
  "name": "zarara",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter rolldown-zarara build:release",
    "build:release": "pnpm --filter rolldown-zarara build:release",
    "build:dev": "pnpm --filter rolldown-zarara build:dev",
    "example": "pnpm --filter rolldown-zarara example",
    "bump": "node scripts/bump-version.mjs",
    "bootstrap:placeholders": "node scripts/bootstrap-placeholders.mjs"
  },
  "engines": {
    "node": ">= 18"
  },
  "packageManager": "pnpm@11.1.2"
}
